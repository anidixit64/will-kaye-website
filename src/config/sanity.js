// Sanity Configuration
// Update these values if you need to connect to a different Sanity project

export const SANITY_CONFIG = {
  projectId: '85zzbmzs',
  dataset: 'production',
  apiVersion: '2023-05-03',
  useCdn: true,
};

// You can also use environment variables if needed:
// export const SANITY_CONFIG = {
//   projectId: process.env.REACT_APP_SANITY_PROJECT_ID || '85zzbmzs',
//   dataset: process.env.REACT_APP_SANITY_DATASET || 'production',
//   apiVersion: process.env.REACT_APP_SANITY_API_VERSION || '2023-05-03',
//   useCdn: true,
// };
